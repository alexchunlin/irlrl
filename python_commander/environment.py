

class RobotEnv():
    def __init__(self,ep_len):

        

    def reset():

    def step(a):
        power_level = a
        send_motor_command(power_level)
        pos_vel_array = receive_reply()

        # process state

        # compute reward
        r = 
        return s, r, d, None         
